#!/bin/bash

apt-get update;
apt-get install -y \
	vim \
	man \
	sudo \
	bat \
	python3 \
	python3-pip \
	curl \
	micro \
	git;

# What would be cool are checkboxes (like when setting up a vue or nuxt project => vue cli),
# to select which packages I will need in this container

# git config ...